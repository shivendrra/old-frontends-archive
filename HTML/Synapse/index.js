let users = ['Tejas', 'Uttam', 'Arpit', 'Preeti'];

function searchfunc(){
    var people, p;
    people = document.getElementById("search1").value;

    if(isNaN(people)){
        while(people == users.includes[p]){
            alert("skjdgfks");
        }
    }
    else{
        alert("choose any user");
    }
}